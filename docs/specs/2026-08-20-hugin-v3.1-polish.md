# Hugin v3.1 — polish backlog

2026-08-20 · Status: backlog approved by Malin (her review of the v3 build); execute next session. v3.0.0 release is HELD until this lands.

## Theme / contrast (workbench + hugin)

1. **Hugin palette, harder**: current look is too soft/cozy — increase background↔text separation so the eye finds what matters; less blending. Tune `hugin.css` in the workbench (canonical), re-extract.
2. **Workbench defaults rework**: "Default", "Daily" and "Hugin" currently look identical. New direction: **Default = something between Kenaz and Gold**; **Daily stays as it is** (its own palette, no longer the default's source). This AMENDS unmerged workbench PR #15 (which made Daily the default) — rework that PR before merge; keep its hugin + classic palettes.
3. **Light mode**: add a dark/light toggle; dark stays default.

## Dashboard

4. "Brreg" → "Brønnøysundregisteret" (both languages).
5. The counts line still says "pipeline" ("1 in pipeline") — leftover from the rename; sweep it (i18n keys).
6. **Dates: same format regardless of language** — "23.08.2026" style (or properly locale-linked; Malin okayed either — pick dd.MM.yyyy everywhere for consistency).
7. **Sources row**: show FINN and LinkedIn links alongside Brønnøysundregisteret and NAV in the header — short labels only ("FINN", "LinkedIn"), not the long linkout text.
8. **Deadlines rows**: date, days-left, category aligned on one clean line/row (current wrap is untidy).
9. **Track from the ad row**: the Active badge only shows for already-tracked companies (confused Malin — IT arkitekt had none because nødnett HF isn't tracked). Add a track/star affordance directly on a Deadlines row so an ad's company can enter Applications from there.
10. **BUG: the "Merk som sett / Mark as seen" confirm dialog renders permanently inline** — the native `<dialog>` lost its hidden state in the design pass (`dialog:not([open])` must not display). Should only appear on button click.

## Applications

11. Contrast fix — sections/entries blend together (same "harder" direction as item 1).
12. **Export filter for Active**: "Active never exports" becomes an option, not a hard rule — extract/export gets a status filter (include Active on demand).

## Companies

13. Text too bright/blurry — reduce brightness/weight (partly caused by ALL-CAPS + heavy font).
14. **Names in Title Case** ("Norsk Tipping AS", not "NORSK TIPPING AS") — display transform (raw stays in db); mind AS/ASA/DA/ANS suffixes staying uppercase.
15. Links on the same row as the company (currently below the card).
16. **Filter: has own website** (checkbox alongside kommune/search).
17. **Group hovedenhet + [branch/avdeling]**: same company shows as two rows (legal entity + underenhet). Group into one row with a branch indicator/expand instead.
18. **Add Larvik kommune** to Malin's config (GET Academy's location) — her `publish\hugin.json`; consider whether default config mentions how.
19. **Standing goal: support every fylke and every municipality in Norway** — the kommune-name register is already synced; this is about the DISCOVERY config scaling (fylke-level selection, all-of-Norway option) without hammering Brreg/NAV.

## Export view

20. Default state EMPTY (no scope preselected, no auto-"All" fetch) — show content only when a scope is chosen deliberately.
21. Align the Download/Copy buttons with the pickers (current placement is off-grid).

## App-wide

22. **NO/EN as ONE synced toggle** (modern segmented control / switch), not two separate buttons.
23. **View-state persistence in-session**: switching views (and coming back) keeps where you were — filters, opened company detail, scroll. Reset to defaults on app restart is fine; preserve during active use. (Views currently unmount and lose state.)
24. **Website from ads**: NAV ad details often carry the employer's real website — during sync, when an ad's employer lacks a website (or has a dead one), take it from the ad detail and feed it through the existing checker. (Verify what the feedentry detail actually exposes — employer.homepage or similar.)
25. **README + GitHub About** refreshed at the end of the wave; document CLI-only usage (hugin.exe standalone) and the localhost API as a machine interface for AI/tooling integration.

## Answers given to Malin (context, no action)

- Show hidden = reveal dismissed ads to un-hide. Active-badge = tracker status per company (see item 9 for the fix). Active-never-exports history = v1 "not outreach yet" rule (item 12 relaxes it). [branch] = Brreg underenhet (item 17 groups it). Single exe exists (`publish-single\Hugin.exe`, unreleased). CLI-only works today.

## Sequencing note

Workbench items (1, 2) first on the existing `feat/hugin-palette` branch (PR #15), then re-extract into hugin-web before the contrast-dependent UI items. Release v3.0.0 (or fold into it) after Malin's next visual verdict.
